# SC11 AI Token Auth 计费修复

## 1. 场景背景与目的

本场景验证 BFE issues #1343、#1344、#1345 的三处计费修复在真实 BFE 进程中的行为：

- **issue #1343**：Anthropic 协议下 `cache_read_input_tokens` 可能远大于 `input_tokens`（后者仅含 cache miss），修复后 `calcChatCost` 不再把 `cacheReadTokens` 截断到 `promptTokens`。
- **issue #1344**：Anthropic `POST /anthropic/v1/messages/count_tokens` 仅用于 token 计数，修复后 `tokenRequestFinishHandler` 跳过该端点，不产生任何费用。
- **issue #1345**：BFE 框架可能多次触发同一请求的 `HandleRequestFinish`，修复后通过请求上下文级 `deducted` 标记保证扣费只执行一次。

## 2. 运行模式

- **单组件模式**：仅启动真实 `bfe` 进程，并启动内存 Redis 服务（由测试框架提供）。
- 不涉及 `ai-gateway-api` 与 `conf-agent`。

## 3. 涉及的 BFE 配置文件

| 文件 | 说明 |
|------|------|
| `bfe.conf` | 启用 `EnableAiGateway`，加载 `mod_ai_route`、`mod_ai_token_auth` |
| `mod_ai_route/mod_ai_route.conf` | 指定 `ai_route.data` 路径 |
| `mod_ai_route/ai_route.data` | 定义 apikey 级路由规则，命中 `cluster_billing_fix` |
| `mod_ai_token_auth/mod_ai_token_auth.conf` | 指定 `token_rule.data` 路径与 Redis 地址 |
| `mod_ai_token_auth/token_rule.data` | 定义 Token、QuotaPlan 与规则 |
| `server_data_conf/host_rule.data` | 声明 `ai_product` 产品线对应的 Host |
| `server_data_conf/route_rule.data` | 默认路由 |
| `cluster_conf/gslb.data` | GSLB 权重配置 |
| `cluster_conf/cluster_table.data` | 运行时根据 mock 后端地址动态生成 |
| `cluster_conf/cluster_conf.data` | 运行时根据 mock 后端动态生成，包含 `cluster_billing_fix` 的 `AIConf` |

## 4. 测试 Cluster 与 AIConf 设计

### 4.1 cluster_billing_fix

配置一个 `claude-opus-4-6` 模型，带有 cache 单价；同时配置一个 `deepseek-chat` 模型用于兜底验证：

```json
{
    "AIConf": {
        "Type": 0,
        "Provider": "mock-provider",
        "Keys": [
            {"Name": "key-primary", "Key": "sk-primary", "Weight": 100}
        ],
        "KeyPolicy": {
            "Strategy": "weighted_random",
            "MaxRetries": 0
        },
        "ModelTable": {
            "Currency": "RMB",
            "Models": [
                {
                    "Provider": "mock-provider",
                    "Model": "claude-opus-4-6",
                    "BaseModel": "claude-opus-4-6",
                    "Mode": "chat",
                    "Capabilities": ["chat"],
                    "Prices": {
                        "input_cost_per_token": 0.00000452,
                        "output_cost_per_token": 0.00002262,
                        "cache_read_input_token_cost": 0.00000045,
                        "cache_creation_input_token_cost": 0.00000565
                    }
                },
                {
                    "Provider": "mock-provider",
                    "Model": "deepseek-chat",
                    "BaseModel": "deepseek-chat",
                    "Mode": "chat",
                    "Capabilities": ["chat"],
                    "Prices": {
                        "input_cost_per_token": 0.000001,
                        "output_cost_per_token": 0.000002
                    }
                }
            ]
        }
    }
}
```

定点单价：
- input = 452
- output = 2262
- cache_read = 45
- cache_write = 565

## 5. Token 与 QuotaPlan 设计

### 5.1 Token 配置

| API Key | 绑定的 QuotaPlan |
|---------|------------------|
| `ak_user_a` | `plan_rmb` |

### 5.2 QuotaPlan 配置

```json
{
    "Id": "plan_rmb",
    "Unlimited": false,
    "PassNoQuota": false,
    "RedisKey": "quota:plan_rmb",
    "ExpiredTime": -1,
    "Quota": 10000000000,
    "Unit": "RMB"
}
```

## 6. 测试例列表

| 编号 | 名称 | 核心验证点 |
|------|------|------------|
| TC-01 | Anthropic 高 cache 命中按实际 cache token 计费 | 后端返回 `input_tokens=320`、`cache_read_input_tokens=8000`，验证按 8000 个 cache token 计费，不再截断到 `promptTokens` |
| TC-02 | count_tokens 端点不计费 | 请求 `POST /anthropic/v1/messages/count_tokens`，验证后端返回 200 但 Redis 余额不变 |
| TC-03 | HandleRequestFinish 重复触发不重复扣费 | 发送一次正常请求，验证 Redis 余额只被扣减一次；重复扣费的直接场景由单元测试覆盖 |

## 6.1 测试例详细说明

### TC-01 Anthropic 高 cache 命中按实际 cache token 计费

**目的**：验证 issue #1343 修复后，`calcChatCost` 不再把 `cacheReadTokens` 截断到 `promptTokens`。

**前置条件**：

- BFE 已启动并加载 `cluster_billing_fix`。
- Redis 中 `quota:plan_rmb` 余额预置为 `10000000000`（100 元）。
- mock 后端配置为返回 Anthropic 标准 usage 响应。

**请求构造**：

| 字段 | 值 |
|------|-----|
| Method | `POST` |
| Host | `billing-fix.example.org` |
| Path | `/v1/chat/completions` |
| Header | `Authorization: Bearer ak_user_a` |
| Header | `Content-Type: application/json` |
| Body | `{"model":"claude-opus-4-6"}` |

**后端响应**：

```json
{
    "usage": {
        "input_tokens": 320,
        "output_tokens": 150,
        "cache_read_input_tokens": 8000,
        "cache_creation_input_tokens": 200
    }
}
```

> 说明：在 Anthropic 协议中，`input_tokens` 仅表示 cache miss 的 token 数；`cache_read_input_tokens` 表示 cache 命中的 token 数，两者之和才是总 prompt token 数。

**执行步骤**：

1. 启动 BFE、mock 后端与内存 Redis。
2. 预置 Redis 余额。
3. 发送 TC-01 请求。
4. 等待异步 Redis 扣费完成（约 500ms）。
5. 读取 Redis 余额并断言。

**预期结果**：

- HTTP 响应状态码为 200。
- `cluster_billing_fix` mock backend 被命中 1 次。
- Redis 余额扣减金额 = `8000*45 + 200*565 + 150*2262 = 812300`（定点）。
- 若按旧逻辑截断到 `promptTokens=320`，扣减金额会显著偏低。

**清理**：

- 停止 BFE、关闭 mock 后端与 Redis。

### TC-02 count_tokens 端点不计费

**目的**：验证 issue #1344 修复后，`tokenRequestFinishHandler` 对 `/count_tokens` 路径跳过扣费逻辑。

**前置条件**：

- BFE 已启动并加载 `cluster_billing_fix`。
- Redis 中 `quota:plan_rmb` 余额预置为 `10000000000`。
- mock 后端配置为返回任意 200 响应体（模拟 count_tokens 结果）。

**请求构造**：

| 字段 | 值 |
|------|-----|
| Method | `POST` |
| Host | `billing-fix.example.org` |
| Path | `/anthropic/v1/messages/count_tokens` |
| Header | `Authorization: Bearer ak_user_a` |
| Header | `Content-Type: application/json` |
| Body | `{"model":"claude-opus-4-6","messages":[{"role":"user","content":"hello"}]}` |

**后端响应**：

```json
{"input_tokens":100}
```

**执行步骤**：

1. 启动 BFE、mock 后端与内存 Redis。
2. 预置 Redis 余额。
3. 发送 TC-02 请求到 `/anthropic/v1/messages/count_tokens`。
4. 等待异步处理完成。
5. 读取 Redis 余额并断言。

**预期结果**：

- HTTP 响应状态码为 200。
- `cluster_billing_fix` mock backend 被命中 1 次。
- Redis 余额保持 `10000000000` 不变。

**清理**：

- 停止 BFE、关闭 mock 后端与 Redis。

### TC-03 HandleRequestFinish 重复触发不重复扣费

**目的**：验证 issue #1345 修复后，单次请求在 `HandleRequestFinish` 阶段的扣费具有幂等性。由于集成测试运行在独立 BFE 进程内，无法直接重复调用框架回调，本 TC 通过断言单次请求的扣费金额来间接验证；直接模拟重复回调的覆盖由单元测试 `TestTokenRequestFinishHandler_NoDuplicateDeduction` 承担。

**前置条件**：

- BFE 已启动并加载 `cluster_billing_fix`。
- Redis 中 `quota:plan_rmb` 余额预置为 `10000000000`。
- mock 后端配置为返回标准 usage 响应。

**请求构造**：

| 字段 | 值 |
|------|-----|
| Method | `POST` |
| Host | `billing-fix.example.org` |
| Path | `/v1/chat/completions` |
| Header | `Authorization: Bearer ak_user_a` |
| Header | `Content-Type: application/json` |
| Body | `{"model":"deepseek-chat"}` |

**后端响应**：

```json
{"usage":{"prompt_tokens":100,"completion_tokens":50,"total_tokens":150}}
```

**执行步骤**：

1. 启动 BFE、mock 后端与内存 Redis。
2. 预置 Redis 余额。
3. 发送 TC-03 请求。
4. 等待异步 Redis 扣费完成。
5. 读取 Redis 余额并断言。

**预期结果**：

- HTTP 响应状态码为 200。
- `cluster_billing_fix` mock backend 被命中 1 次。
- Redis 余额扣减金额 = `100*100 + 50*200 = 20000`（定点）。
- 若存在重复扣费 bug，实际扣减金额会大于 20000。

**补充说明**：

- 单元测试中通过直接调用 `tokenRequestFinishHandler(req, res)` 两次，并校验 mock Redis 只扣减一次，完整覆盖 `deducted` 标记的幂等行为。

**清理**：

- 停止 BFE、关闭 mock 后端与 Redis。

## 7. 公共基础设施

本场景复用 SC03 的公共测试框架能力：

1. **`common/redis_server.go`**：启动内存 Redis 服务。
2. **`common/bfe_config_builder.go`**：动态生成 BFE 配置。
3. **`common/mock_backend.go`**：模拟 AI 后端响应。

## 8. 依赖与风险

| 风险点 | 说明 | 缓解措施 |
|--------|------|----------|
| 需要验证 Anthropic 字段解析 | `cache_read_input_tokens` 由 `UpdateCtxByUsage` 解析 | 响应体使用标准 Claude usage 字段 |
| `/count_tokens` 路径匹配 | BFE 使用 `RequestURI` 做子串匹配 | 测试中构造完整路径 |
| 重复回调模拟 | 集成测试里框架不会真实触发两次 `HandleRequestFinish` | 通过直接调用 handler 模拟 |

## 9. 与其他场景的依赖关系

- 依赖 SC01 已验证的 `mod_ai_route` 路由表查找与绑定能力；
- 依赖 SC03 已验证的 RMB 配额扣减框架；
- 本场景 focus 在 `mod_ai_token_auth` 的三处具体计费修复。
