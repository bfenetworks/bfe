# TC-03 完整流式响应计费不变

## 目的

回归保护：修复 #1352 后，正常完成的完整 SSE 流式响应仍按实际 usage 计费，行为与修复前一致。

对应实现：`TestTC03_CompleteStreamBillsActualUsage`。

## 前置条件

- 同 TC-01。

## 请求构造

同 TC-01。

## 后端响应（SSE）

```
data: {"type":"message_start","message":{"id":"msg_01","type":"message","usage":{"input_tokens":320,"output_tokens":0}}}

data: {"type":"content_block_delta","index":0,"delta":{"type":"text_delta","text":"hello"}}

data: {"type":"message_delta","delta":{"stop_reason":"end_turn"},"usage":{"output_tokens":150}}}

data: {"type":"message_stop"}
```

## 执行步骤

1. 客户端发送请求并读取完整 SSE 流（直至 `message_stop`）。
2. 正常关闭连接。
3. 读取 Redis 余额并断言。

## 预期结果

- HTTP 响应状态码为 200。
- Redis 余额扣减金额 = `320*452 + 150*2262 = 483940`（定点）。

## 补充说明

本 TC 与 TC-02 的扣费金额一致，构成对照：同样收到最终 usage，TC-03 正常读完，TC-02 中断于最终 usage 之后，两者均应精确按实际 usage 扣费。

## 清理

- 停止 BFE、关闭 mock 后端与 Redis。
